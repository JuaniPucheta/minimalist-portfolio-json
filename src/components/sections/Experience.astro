---
import Section from "../Section.astro";

import { work } from "@cv"
---

<Section title="Experiencia laboral">
    <ul>
        {
            work.map(({name, startDate, endDate, position, summary, tipo, url}) => {
                const endYear = endDate ? new Date(endDate).getFullYear() : "Actualmente"
                return(
                    <li>
                        <article>
                            <header>
                                <div>
                                    <h3>
                                        <a href={url} target="_blank" rel="noopener noreferrer">
                                            {name} - <span>{ tipo }</span>
                                        </a>
                                    </h3>
                                    <p class="posicion">{position}</p>
                                </div>
                                <time>
                                    {startDate} - {endYear}
                                </time>
                            </header>

                            <footer>
                                <p>{summary}</p>
                            </footer>
                        </article>
                    </li>
                )}
            )
        }
    </ul>
</Section>

<style>
    ul {
        display: flex;
        flex-direction: column;
        gap: 32px;
    }

    article h3 {
        font-weight: 500;
        color: #111;
        transition: color 0.3s ease, transform 0.3s ease;
    }

    article h3:hover {
        color: #4e4e4e;
        transform: scale(1.05);
    }

    article h4 {
        font-weight: 400;
        color: #222;
    }

    header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 4px;
    }

    time {
        color: #555;
        font-size: 0.85rem;
    }

    span {
        border-radius: 6px;
        background-color: #eee;
        color: black;
        font-size: .8rem;
        font-weight: 500;
        padding: 0.2rem 0.5rem;
    }
</style>
